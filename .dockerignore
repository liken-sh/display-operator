# The build needs the module files, the Go sources, and the script
# that collects the compositor. Everything else is documentation,
# manifests, or test fixtures.
.git
.github
deploy
testdata
README.md
LICENSE
